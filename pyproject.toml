[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Hey, I just met you..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.13.2",
    "llm_sdk @ file:///${PROJECT_ROOT}/src/llm_sdk",
    "flake8>=7.3.0",
    "mypy>=1.20.1",
]